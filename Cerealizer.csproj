<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <PackOnBuild>true</PackOnBuild>
    <PackageVersion>0.0.2</PackageVersion>
    <Authors>Ian Knighton</Authors>
    <Copyright>2020</Copyright>
    <Owners>Ian Knighton</Owners>
    <PackageProjectUrl>https://github.com/IanKnighton/Cerealizer</PackageProjectUrl>
    <Summary>It's wrapper for JSON serializing, but it uses the word cereal. That's it.</Summary>
    <Title>Cerealizer</Title>
    <Description>It's wrapper for JSON serializing, but it uses the word cereal. That's it.</Description>
    <RepositoryUrl>https://github.com/IanKnighton/Cerealizer</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="12.0.3" />
  </ItemGroup>
</Project>
